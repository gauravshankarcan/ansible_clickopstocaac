{% raw %}
---
# Reference only — connection vars are set from environment in apply.yml playbook vars.
# Export these before running apply:
#   export AAP_URL / AAP_BASE
#   export AAP_USER / AAP_PASS  (or AAP_TOKEN)
#   export AAP_VALIDATE_CERTS=false
{% endraw %}
